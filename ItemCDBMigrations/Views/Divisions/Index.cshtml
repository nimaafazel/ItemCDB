@model IEnumerable<ItemCDBMigrations.Models.tblDivision>

@{
    ViewBag.Title = "Index";
}

<h2>Divisions</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DivDesc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DivDesc1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DivChief)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DivDesc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DivDesc1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DivChief)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DivCode }) |
            @*@Html.ActionLink("Details", "Details", new { id=item.DivCode }) |*@
            @Html.ActionLink("Delete", "Delete", new { id=item.DivCode })
        </td>
    </tr>
}

</table>
