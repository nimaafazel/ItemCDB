@model IEnumerable<ItemCDBMigrations.Models.tblSection>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SecDesc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SecDesc1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SecHead)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SecDesc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SecDesc1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SecHead)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SecCode }) |
            @Html.ActionLink("Details", "Details", new { id=item.SecCode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SecCode })
        </td>
    </tr>
}

</table>
